{{#capture-content
  triggerValue
  project.structure
  project.type
  entityType
  update=(action (mut source))
}}
  {{yield}}
{{/capture-content}}

{{code-block filename=effectivePath source=source}}
